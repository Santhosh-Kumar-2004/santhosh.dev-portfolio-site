import { BrowserRouter, Routes, Route } from "react-router-dom";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* Pages will be added one by one */}
      </Routes>
    </BrowserRouter>
  );
}

export default App;
